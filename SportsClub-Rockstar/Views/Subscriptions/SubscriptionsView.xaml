<Page x:Class="Rockstar.Admin.WPF.Views.Subscriptions.SubscriptionsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:controls="clr-namespace:Rockstar.Admin.WPF.Views.Subscriptions.Controls"
      mc:Ignorable="d"
      d:DesignHeight="750" d:DesignWidth="1100"
      Background="#F5F7FA"
      FontFamily="Segoe UI"
      Title="SubscriptionsView"
      x:Name="SubscriptionsPage">

    <Page.Resources>
        <Style x:Key="PageTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2C2C2E"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF4D00"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="24,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E64500"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E8E8ED"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,0"/>
        </Style>

        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E8E8ED"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="12,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ===== ВЕРХНЯЯ ПАНЕЛЬ: Заголовок и кнопки ===== -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Абонементы" Style="{StaticResource PageTitleStyle}"/>
                <TextBlock Text="Управление абонементами и их стоимостью"
                           FontSize="14"
                           Foreground="#8E8E93"
                           Margin="0,4,0,0"/>
            </StackPanel>

            <Button Grid.Column="1"
                    Content="Обновить"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding RefreshCommand}"
                    Background="White" Foreground="#FF4D00"
                    BorderBrush="#FF4D00" BorderThickness="1"
                    Width="100" Margin="0,0,10,0"/>

            <Button Grid.Column="2"
                    Content="+ Добавить"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding AddSubscriptionCommand}"
                    Width="120" Margin="0,0,10,0"/>

            <Button Grid.Column="3"
                    Content="Назад"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding BackCommand}"
                    Width="100"/>
        </Grid>

        <!-- ===== ПАНЕЛЬ ФИЛЬТРА И ПОИСКА ===== -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Фильтр по направлению -->
            <ComboBox Grid.Column="0"
                      ItemsSource="{Binding Directions}"
                      SelectedItem="{Binding SelectedDirectionFilter}"
                      DisplayMemberPath="Name"
                      Style="{StaticResource FilterComboBoxStyle}"/>

            <!-- Поле поиска -->
            <TextBox Grid.Column="2"
                     Style="{StaticResource SearchBoxStyle}"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>

        <!-- ===== ОТЛАДОЧНАЯ ИНФОРМАЦИЯ: Счётчики ===== -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Всего абонементов: " Foreground="#666666"/>
            <TextBlock Text="{Binding AllSubscriptionsCount}" Foreground="#FF4D00" FontWeight="Bold" Margin="0,0,20,0"/>
            <TextBlock Text="Отфильтровано: " Foreground="#666666"/>
            <TextBlock Text="{Binding FilteredSubscriptionsCount}" Foreground="#FF4D00" FontWeight="Bold" Margin="0,0,20,0"/>
            <TextBlock Text="Фильтр: " Foreground="#666666"/>
            <TextBlock Text="{Binding SelectedDirectionFilter.Name, TargetNullValue='Все'}" Foreground="#FF4D00" FontWeight="Bold"/>
        </StackPanel>

        <!-- ===== ИНДИКАТОР ЗАГРУЗКИ ===== -->
        <Grid Grid.Row="3" Visibility="{Binding LoadingVisibility}" VerticalAlignment="Center">
            <StackPanel HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="300" Height="4" Foreground="#FF4D00"/>
                <TextBlock Text="Загрузка абонементов..."
                           FontSize="14" Foreground="#8E8E93"
                           Margin="0,16,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- ===== СПИСОК КАРТОЧЕК АБОНЕМЕНТОВ ===== -->
        <ScrollViewer Grid.Row="3"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Visibility="{Binding ContentVisibility}">
            <ItemsControl ItemsSource="{Binding Subscriptions}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:SubscriptionCardControl
                            Subscription="{Binding}"
                            EditCommand="{Binding DataContext.EditSubscriptionCommand, ElementName=SubscriptionsPage}"
                            DeleteCommand="{Binding DataContext.DeleteSubscriptionCommand, ElementName=SubscriptionsPage}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>