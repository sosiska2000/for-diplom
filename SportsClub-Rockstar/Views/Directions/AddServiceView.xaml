<Page x:Class="Rockstar.Admin.WPF.Views.Directions.AddServiceView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="600"
      Background="#F5F7FA"
      FontFamily="Segoe UI"
      Title="AddServiceView">

    <Page.Resources>
        <Style x:Key="FormLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#2C2C2E"/>
            <Setter Property="Margin" Value="0,16,0,6"/>
        </Style>
        <Style x:Key="FormInputStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E8E8ED"/>
            <Setter Property="Height" Value="44"/>
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF4D00"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E64500"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E8E8ED"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ErrorTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#FF4D00"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>
    </Page.Resources>

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="{Binding PageTitle}"
                       FontSize="24" FontWeight="SemiBold" Foreground="#2C2C2E"/>
            <TextBlock Text="{Binding DirectionTitle}"
                       FontSize="14" Foreground="#8E8E93" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Форма -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,24,0,0">
            <StackPanel>
                <!-- Название -->
                <TextBlock Text="Название услуги" Style="{StaticResource FormLabelStyle}"/>
                <Border Style="{StaticResource FormInputStyle}">
                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0" Background="Transparent" FontSize="14"
                             VerticalContentAlignment="Center" Padding="12,0"/>
                </Border>
                <TextBlock Text="{Binding NameError}" Style="{StaticResource ErrorTextStyle}"/>

                <!-- Стоимость -->
                <TextBlock Text="Стоимость (₽)" Style="{StaticResource FormLabelStyle}"/>
                <Border Style="{StaticResource FormInputStyle}">
                    <TextBox Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0" Background="Transparent" FontSize="14"
                             VerticalContentAlignment="Center" Padding="12,0"
                             PreviewTextInput="NumberValidationTextBox"/>
                </Border>
                <TextBlock Text="{Binding PriceError}" Style="{StaticResource ErrorTextStyle}"/>

                <!-- Количество занятий -->
                <TextBlock Text="Количество занятий" Style="{StaticResource FormLabelStyle}"/>
                <Border Style="{StaticResource FormInputStyle}">
                    <TextBox Text="{Binding SessionsCount, UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0" Background="Transparent" FontSize="14"
                             VerticalContentAlignment="Center" Padding="12,0"
                             PreviewTextInput="NumberValidationTextBox"/>
                </Border>

                <!-- Длительность -->
                <TextBlock Text="Длительность (мин)" Style="{StaticResource FormLabelStyle}"/>
                <Border Style="{StaticResource FormInputStyle}">
                    <TextBox Text="{Binding DurationMinutes, UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0" Background="Transparent" FontSize="14"
                             VerticalContentAlignment="Center" Padding="12,0"
                             PreviewTextInput="NumberValidationTextBox"/>
                </Border>

                <!-- Описание -->
                <TextBlock Text="Описание" Style="{StaticResource FormLabelStyle}"/>
                <Border CornerRadius="8" Background="White" BorderThickness="1" BorderBrush="#E8E8ED"
                        Padding="12" MinHeight="100">
                    <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0" Background="Transparent" FontSize="14"
                             TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
            <Button Content="Отмена" Style="{StaticResource SecondaryButtonStyle}"
                    Width="120" Margin="0,0,12,0" Command="{Binding CancelCommand}"/>
            <Button Content="Сохранить" Style="{StaticResource PrimaryButtonStyle}"
                    Width="120" Command="{Binding SaveCommand}" IsEnabled="{Binding CanSave}"/>
        </StackPanel>
    </Grid>
</Page>