<UserControl x:Class="Rockstar.Admin.WPF.Views.Directions.Controls.ServiceTypeCardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="250"
             x:Name="ServiceTypeCard" Height="222">

    <UserControl.Resources>
        <Style x:Key="CardButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FF4D00"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFF5F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DeleteButtonStyle" TargetType="Button" BasedOn="{StaticResource CardButtonStyle}">
            <Setter Property="Foreground" Value="#FF5252"/>
        </Style>
    </UserControl.Resources>

    <Border Background="White" 
            CornerRadius="8" 
            BorderThickness="1" 
            BorderBrush="#E8E8ED"
            Padding="16"
            Margin="4,0,0,0">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.02" BlurRadius="8" Direction="270" ShadowDepth="2"/>
        </Border.Effect>

        <StackPanel>
            <!-- Название типа -->
            <TextBlock Text="{Binding TypeName, ElementName=ServiceTypeCard}" 
                       FontSize="15" FontWeight="SemiBold"
                       Foreground="#2C2C2E"
                       Margin="0,0,0,6"/>

            <!-- Описание -->
            <TextBlock Text="{Binding TypeDescription, ElementName=ServiceTypeCard}" 
                       FontSize="12" Foreground="#666666"
                       TextWrapping="Wrap"
                       MaxHeight="40"
                       Margin="0,0,0,8"/>

            <!-- Длительность по умолчанию -->
            <TextBlock Text="{Binding DurationText, ElementName=ServiceTypeCard}" 
                       FontSize="12" Foreground="#8E8E93"
                       Margin="0,0,0,12"/>

            <!-- Кнопки действий -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Редактировать"
                        Style="{StaticResource CardButtonStyle}"
                        Command="{Binding EditCommand, ElementName=ServiceTypeCard}"
                        CommandParameter="{Binding ServiceType, ElementName=ServiceTypeCard}"
                        Margin="0,0,8,0"/>
                <Button Content="Удалить"
                        Style="{StaticResource DeleteButtonStyle}"
                        Command="{Binding DeleteCommand, ElementName=ServiceTypeCard}"
                        CommandParameter="{Binding ServiceType, ElementName=ServiceTypeCard}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>