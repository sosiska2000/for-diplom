<Window x:Class="Rockstar.Admin.WPF.Views.Directions.EditServiceTypeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вид занятий" 
        Width="500" Height="450"
        WindowStartupLocation="CenterOwner"
        Background="#F5F7FA">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   x:Name="tbTitle"
                   FontSize="20" 
                   FontWeight="SemiBold" 
                   Foreground="#2C2C2E"
                   Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Text="Название вида занятий" FontSize="14" Foreground="#2C2C2E" Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" x:Name="tbName" 
                 Height="40" 
                 Padding="12,0" 
                 FontSize="14"
                 Background="White" 
                 BorderBrush="#E8E8ED" 
                 BorderThickness="1"
                 Margin="0,0,0,15"/>

        <TextBlock Grid.Row="3" Text="Длительность по умолчанию (минут)" FontSize="14" Foreground="#2C2C2E" Margin="0,0,0,5"/>
        <TextBox Grid.Row="4" x:Name="tbDefaultDuration" 
                 Height="40" 
                 Padding="12,0" 
                 FontSize="14"
                 Background="White" 
                 BorderBrush="#E8E8ED" 
                 BorderThickness="1"
                 Margin="0,0,0,15"
                 PreviewTextInput="NumberValidationTextBox"/>

        <TextBlock Grid.Row="5" Text="Описание" FontSize="14" Foreground="#2C2C2E" Margin="0,0,0,5"/>
        <TextBox Grid.Row="6" x:Name="tbDescription" 
                 Height="80" 
                 Padding="12,8" 
                 FontSize="14"
                 Background="White" 
                 BorderBrush="#E8E8ED" 
                 BorderThickness="1"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,20"/>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" 
                    Width="100" Height="40" 
                    Margin="0,0,10,0"
                    Click="Cancel_Click"
                    Cursor="Hand"/>
            <Button Content="Сохранить" 
                    Width="100" Height="40" 
                    Background="#FF4D00" 
                    Foreground="White"
                    Click="Save_Click"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>