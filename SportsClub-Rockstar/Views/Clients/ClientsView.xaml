<Page x:Class="Rockstar.Admin.WPF.Views.Clients.ClientsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:controls="clr-namespace:Rockstar.Admin.WPF.Views.Clients.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="750" d:DesignWidth="1100"
      Background="#F5F7FA"
      Title="ClientsView"
      x:Name="ClientsPage">

    <Page.Resources>
        <Style x:Key="PageTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2C2C2E"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF4D00"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="24,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E64500"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E8E8ED"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="12,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Учетные записи клиентов" Style="{StaticResource PageTitleStyle}"/>
                <TextBlock Text="Управление профилями клиентов клуба" 
                           FontSize="14" 
                           Foreground="#8E8E93"
                           Margin="0,4,0,0"/>
            </StackPanel>

            <Button Grid.Column="1"
                    Content="Обновить"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding RefreshCommand}"
                    Background="White" Foreground="#FF4D00"
                    BorderBrush="#FF4D00" BorderThickness="1"
                    Width="100" Margin="0,0,10,0"/>

            <Button Grid.Column="2"
                    Content="+ Добавить"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding AddClientCommand}"
                    Width="120" Margin="0,0,10,0"/>

            <Button Grid.Column="3"
                    Content="Назад" 
                    Style="{StaticResource PrimaryButtonStyle}" 
                    Command="{Binding BackCommand}"
                    Width="100"/>
        </Grid>

        <!-- Панель поиска -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <TextBox Style="{StaticResource SearchBoxStyle}"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>

        <!-- Отладочная информация -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Всего клиентов: " Foreground="#666666"/>
            <TextBlock Text="{Binding Clients.Count}" Foreground="#FF4D00" FontWeight="Bold" Margin="0,0,20,0"/>

            <TextBlock Text="Отфильтровано: " Foreground="#666666"/>
            <TextBlock Text="{Binding FilteredClients.Count}" Foreground="#FF4D00" FontWeight="Bold" Margin="0,0,20,0"/>

            <TextBlock Text="Поиск: " Foreground="#666666"/>
            <TextBlock Text="{Binding SearchText, TargetNullValue=''}" Foreground="#FF4D00" FontWeight="Bold"/>
        </StackPanel>

        <!-- Индикатор загрузки -->
        <Grid Grid.Row="3" Visibility="{Binding LoadingVisibility}" VerticalAlignment="Center">
            <StackPanel HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="300" Height="4" Foreground="#FF4D00"/>
                <TextBlock Text="Загрузка клиентов..." 
                           FontSize="14" Foreground="#8E8E93" 
                           Margin="0,16,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Список клиентов -->
        <ScrollViewer Grid.Row="3" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Visibility="{Binding ContentVisibility}">
            <ItemsControl ItemsSource="{Binding FilteredClients}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:ClientCard 
                            Client="{Binding}"
                            EditCommand="{Binding DataContext.EditClientCommand, ElementName=ClientsPage}"
                            DeleteCommand="{Binding DataContext.DeleteClientCommand, ElementName=ClientsPage}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>